const colorize = (msg, color) => process.stdout.isTTY ? `${color}${msg}\x1b[0m` : msg;
const magenta = (msg) => colorize(msg, '\x1b[35m');
const green = (msg) => colorize(msg, '\x1b[32m');
export { magenta, green };
